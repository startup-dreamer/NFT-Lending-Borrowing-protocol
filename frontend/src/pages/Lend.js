import React from 'react';
import {deposit_to_pool, withdraw_to_pool} from '../backend'

const Lend = ({Contract}) => {
    return (
        <div>
            
        </div>
    );
}

export default Lend;
